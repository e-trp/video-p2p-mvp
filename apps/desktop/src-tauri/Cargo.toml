[package]
name = "desktop-app"
version = "0.1.0"
edition = "2024"
description = "Tauri desktop shell for the video-p2p project"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
app-core = { path = "../../../crates/app-core" }
serde = { version = "1", features = ["derive"] }
tauri = { version = "2", features = [] }

[features]
default = []
